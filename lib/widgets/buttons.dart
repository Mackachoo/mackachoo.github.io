import 'package:flutter/material.dart';

class EnquiryButton extends ElevatedButton {
  EnquiryButton({super.key})
      : super(
          onPressed: null,
          child: Text("Enquiries".toUpperCase()),
        );
}
